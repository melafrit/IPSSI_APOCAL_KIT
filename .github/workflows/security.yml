name: Security tests

on:
  push:
  pull_request:

jobs:
  prompt-injection-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create env file
        run: cp .env.example .env

      - name: Run prompt injection tests
        run: docker compose run --rm backend pytest /app/llm/tests/test_prompt_injection.py